<UserControl x:Class="Huoyaoyuan.AdmiralRoom.Controls.AnimateProgress"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:p="clr-namespace:System.Windows.Controls.Primitives;assembly=PresentationCore"
             xmlns:local="clr-namespace:Huoyaoyuan.AdmiralRoom.Controls"
             Foreground="Green"
             x:Name="control"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <ProgressBar x:Name="bar"
                 Background="{Binding Background, ElementName=control}"
                 Foreground="{Binding Foreground, ElementName=control}"
                 Minimum="{Binding Minimum, ElementName=control}"
                 Maximum="{Binding Maximum, ElementName=control}"/>
</UserControl>
