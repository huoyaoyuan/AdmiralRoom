<UserControl x:Class="Huoyaoyuan.AdmiralRoom.Controls.ExpBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Huoyaoyuan.AdmiralRoom.Controls"
             mc:Ignorable="d">
    <UserControl.ToolTip>
        <TextBlock x:Name="ToolTipText"/>
    </UserControl.ToolTip>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="6"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock FontSize="18" Text="{Binding Level, RelativeSource={RelativeSource AncestorType={x:Type local:ExpBar}}, StringFormat=Lv. {0}}"/>
        <local:AnimateProgress x:Name="BarExp" Grid.Row="1" Background="Transparent"/>
        <TextBlock FontSize="12" Grid.Row="2" Text="{Binding Exp.Next, RelativeSource={RelativeSource AncestorType={x:Type local:ExpBar}}, StringFormat=Next. {0}}"/>
    </Grid>
</UserControl>
